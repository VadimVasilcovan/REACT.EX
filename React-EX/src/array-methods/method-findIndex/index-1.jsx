export default function FindIndexMethod () {

    const numbers = [10, 20, 30, 40];

    const index1 = numbers.findIndex((num) => num ===20)

    console.log(index1)


    const index2 = numbers.findIndex((num) => num ===12)
    console.log(index2)

    return(<div>

    </div>)
}